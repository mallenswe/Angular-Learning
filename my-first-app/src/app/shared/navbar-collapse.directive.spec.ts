import { NavbarCollapseDirective } from './navbar-collapse.directive';

describe('NavbarCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
